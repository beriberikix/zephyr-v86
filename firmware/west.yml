manifest:
  remotes:
    - name: zephyr
      url-base: https://github.com/zephyrproject-rtos
  projects:
    - name: zephyr
      remote: zephyr
      revision: v4.3.0
      path: zephyr
      import:
        # Keep imports minimal for this qemu_x86 shell-focused app.
        # Add project names here only if future features require them.
        name-allowlist:
          - cmsis
